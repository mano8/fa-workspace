#!/bin/sh
set -eu

if git diff --cached --name-only --diff-filter=ACMR | grep -Eq '(^|/)(todo|\.workspace/status)(/|$)'; then
  echo 'Commit blocked: TODO plans and .workspace/status artifacts must remain local.' >&2
  exit 1
fi
