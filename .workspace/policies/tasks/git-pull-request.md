# Pull-request task

- Return a proposed pull-request description in a fenced `markdown` block so
  it is directly copyable.
- Do not add attribution footers or identify the tool that produced commit or
  pull-request text.
- Pull-request authorization does not authorize branch creation, committing,
  pushing, or releasing.
