# Release task

A release is child-owned and mutating. Operate only on the explicitly selected
repository under recorded human authorization for the exact release operation;
use that child's release procedure and quality gates. Selecting this overlay
does not authorize branch creation, commits, pushes, pull requests, or changes
to another child repository.
