"""Inactive W2b agent-context primitives.

The package is intentionally additive while v1 workspace configuration remains
authoritative.  The W2b3 client-neutral delivery kernel adds isolated runtime
state and a fake adapter only. The W5 Claude boundary is fail-closed because no
current Claude hook or real transport is capability-supported; Codex launchers
and all real client activation remain later work.
"""
